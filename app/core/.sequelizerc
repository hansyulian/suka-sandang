const path = require("path");

const rc = {
  config: path.resolve(__dirname, "../core/config/config.js"),
  "migrations-path": path.resolve(__dirname, "../core/migrations"),
};
console.log("sequelizerc", rc);
module.exports = rc;
