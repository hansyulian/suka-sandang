{
  "name": "hyulian",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "api": "pnpm --filter ./app/api dev",
    "web": "pnpm --filter ./app/web dev",
    "migrate": "pnpm --filter ./app/engine migrate",
    "migrate:reset": "pnpm --filter ./app/engine migrate:reset",
    "watch": "node scripts/watch-all.js",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test",
    "integrity-test": "pnpm build && pnpm test",
    "clean": "node scripts/clean.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^6.0.1"
  }
}
